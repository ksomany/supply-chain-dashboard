# PostgreSQL connection (individual params — avoids URL-encoding issues with special chars in passwords)
PGHOST=your-db-host
PGPORT=5432
PGDATABASE=your-db-name
PGUSER=your-db-user
PGPASSWORD=your-db-password

PORT=3001

# Dashboard login credentials (used in production only)
DASHBOARD_USER=admin
DASHBOARD_PASS=choose-a-strong-password
SESSION_SECRET=random-32-char-string-change-this
